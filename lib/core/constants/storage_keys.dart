/// Keys for persisted app state (SharedPreferences, etc.).
abstract final class StorageKeys {
  /// Small app-level startup flag.
  static const String resourcesDownloaded = 'app_resources_downloaded';
}
